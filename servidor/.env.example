PORT=3001
CORS_ORIGIN=*

DB_HOST=localhost
DB_PORT=3307
DB_USER=root
DB_PASSWORD=
DB_NAME=pizarra_db

# Secreto para firmar los JWT de autenticación. Usa una cadena larga y aleatoria.
JWT_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio
JWT_EXPIRES_IN=7d

# Si un usuario se registra con este email, se le asigna el rol "admin" automáticamente
ADMIN_EMAIL=
